import { Sandbox } from "./vendor/sandbox";
import { HubConnection } from "@aspnet/signalr";

export const showRoomStatus = (config: { baseURL: string, room: string, connection: HubConnection }) => {

}
